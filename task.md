so we do validation for social media each provider.

that validation are mainle checkign video limie,  video size, post text size, etc, 

so i want write test for them. So test will follow current framework and sytel of writng test in postiz.

it will have testcases and testscript. 

i think these validation are triggered in @libraries\nestjs-libraries\src\chat\tools\integration.schedule.post.ts
i am not sure.
we dont need write dupolicate theings agin we should taest it in such way thtat we used it in our curent codebase.
so first find how are we vcalidating and the write test.

for now we will keep the code short and for 1 provider.

write needed code to write those test.
for now we will only focus on fone priovider which is discord. but wer should write code in such weay that we will add more test for other providers as well.
so our code should be reusable , clean and prod ready simple short .

write/edit needed code only. dont assume anything.


